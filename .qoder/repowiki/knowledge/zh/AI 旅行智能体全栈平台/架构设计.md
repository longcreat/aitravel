- **服务编排**：根目录通过 `docker-compose.aliyun.yml` 定义后端服务的构建上下文、端口映射（127.0.0.1:18000）及数据卷挂载，实现基础设施即代码。
- **配置协同**：前后端共享 `.env` 环境变量管理模式，并通过 `backend/config/mcp.servers.json` 统一管理 MCP 协议连接器配置，确保智能体能力的一致性。
- **部署流水线**：利用 `.tmp` 目录下的打包产物（tgz）及 `deploy/aliyun` 脚本，实现了从本地开发到阿里云 1Panel/OpenResty 环境的自动化同步与证书管理。