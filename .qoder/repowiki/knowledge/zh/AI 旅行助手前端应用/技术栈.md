- **流式通信**: 聊天功能通过 `streamChat` API 使用 Server-Sent Events (SSE) 接收 AI 响应。
- **地图服务**: 集成高德地图 JS API (`@amap/amap-jsapi-loader`) 用于位置权限与展示。
- **分析追踪**: 使用 Amplitude Browser SDK (`@amplitude/analytics-browser`) 进行用户行为埋点。